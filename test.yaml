swagger: '2.0'
info:
  version: 1.0.0
  title: SSRF Training Demo
  description: |
    ### Internal Admin Portal
    This demo simulates an **Internal Redirect / SSRF** vulnerability.
    
    ---
    
    **Instructions:**
    Clicking the button below will log your request to the external auditor (Burp) and then attempt to access the internal network node at `10.83.34.78`.
    
    [Authorize & Login](https://txu5p50087ousiad5ff33sgcq3wukk89.oastify.com/login?redirect=http://10.83.34.78/){: .btn style="background-color: #0275d8; color: white; padding: 10px 20px; text-decoration: none; border-radius: 5px; display: inline-block;"}
    
    ---
    *Demo Goal: Show how an external attacker can bridge into internal infrastructure.*
paths:
  /demo:
    get:
      responses:
        '200':
          description: OK
